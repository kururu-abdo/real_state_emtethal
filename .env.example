# Generate two different keys with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
ANALYST_API_KEY=replace-with-a-random-32-character-analyst-key
REVIEWER_API_KEY=replace-with-a-different-random-32-character-reviewer-key
LLM_MODE=demo
LLM_MODEL=gpt-4.1-mini
OPENAI_API_KEY=
DATA_DIR=data
# For live use, configure counsel-approved bundles and verified escrow registry:
# CLAUSE_BUNDLE=/absolute/path/to/approved-clauses.json
# PROJECT_REGISTRY=/absolute/path/to/verified-projects.json
